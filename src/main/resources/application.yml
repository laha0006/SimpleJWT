spring:
  application:
    name: SimpleJWT
  datasource:
    url: jdbc:mysql://localhost:13306/jpa_test
    username: root
    password: root
  jpa:
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    open-in-view: false
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: Ov23li0ZIdxr4CdjpeFI    # throw-away  test app
            client-secret: dfa931fc86cb9758249f6ef063710565e6292c7b   # throw-away test app
#            scope: read:user, user:email
#            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
#            authorization-grant-type: authorization_code
#            client-name: GitHub
        provider:
          github:
#            authorization-uri: https://github.com/login/oauth/authorize
#            token-uri: https://github.com/login/oauth/access_token
#            user-info-uri: https://api.github.com/user
            user-name-attribute: login



logging:
  level:
    org:
      springframework:
        security:
          oauth2: trace


#logging.level.org.springframework.security.oauth2

rsa:
  public-key: classpath:certs/public.pem
  private-key: classpath:certs/private.pem